# dcproject

dcproject (- distributed computers project) is a small project in the feild of **Super Computing**, designed to improve user's computer CPU power by sharing tasks with other computers.

## Installing 

To install all required packages with the exact versions:

open bash and type:
"pip install -r requirements.txt"